import React from "react";

import "./FilterFieldContainer.css";

const SolutionContainer = (props) => {
  return <div id="filter-container">{props.children}</div>;
};

export default SolutionContainer;
